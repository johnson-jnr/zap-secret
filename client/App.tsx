import { BrowserRouter } from 'react-router';
import RoutesConfig from './RoutesConfig';

function App() {
    return (
        <BrowserRouter>
            <RoutesConfig />
        </BrowserRouter>
    );
}

export default App;
